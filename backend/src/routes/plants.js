const express = require('express');
const router = express.Router();

// Placeholder for plants API - will implement in next commit
router.get('/', (req, res) => {
  res.json({ message: 'Plants API coming soon' });
});

module.exports = router;

