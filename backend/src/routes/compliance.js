const express = require('express');
const router = express.Router();

// Placeholder for compliance API - will implement in next commit
router.get('/', (req, res) => {
  res.json({ message: 'Compliance API coming soon' });
});

module.exports = router;

