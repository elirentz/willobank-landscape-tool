const express = require('express');
const router = express.Router();

// Placeholder for phases API - will implement in next commit
router.get('/', (req, res) => {
  res.json({ message: 'Phases API coming soon' });
});

module.exports = router;

